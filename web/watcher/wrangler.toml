name = "bilardo-watcher"
main = "src/index.js"
compatibility_date = "2026-07-01"

# poll bilardo.info every 5 minutes
[triggers]
crons = ["*/5 * * * *"]

[[kv_namespaces]]
binding = "STATE"
id = "1524ee24bd53494b818cc5aa043c07a8"

[vars]
# subscribe at https://ntfy.sh/bilardo-watch-95271fa2aa3f (app or browser);
# set to "" to disable. WEBHOOK_URL can be added as a secret for a custom hook:
#   npx wrangler secret put WEBHOOK_URL
NTFY_TOPIC = "bilardo-watch-95271fa2aa3f"
