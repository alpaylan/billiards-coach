# Shot reports

Files here are written by the editor's 🚩 Report button (one `report_<unix>.md`
per report: shot path, optional bad time segment, ball + corrected position,
free text). The assistant watches this folder and picks reports up
automatically — no need to paste them into the chat.
